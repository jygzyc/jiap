package net.sqlcipher.database;

/** In-tree stub: SQLCipher openOrCreateDatabase with passphrase. */
public class SQLiteDatabase {
    public static SQLiteDatabase openOrCreateDatabase(String path, String password, Object factory) {
        return new SQLiteDatabase();
    }
}
